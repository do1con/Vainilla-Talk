import React from 'react';
import { Layout } from 'antd';

const { Header } = Layout;

const Head = function () {
  return (
    <Header>
      <h1>hi.</h1>
    </Header>
  );
};

export default Head;
